<div class="container" role="main">
  <h1>Workflow Demo - Patterns</h1>

  {{#if useAnimations}}
    {{liquid-outlet}}
  {{else}}
    {{outlet}}
  {{/if}}

  <p>Back to {{#link-to 'process-defs'}}process list{{/link-to}}</p>
</div>
